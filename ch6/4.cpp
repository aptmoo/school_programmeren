#include <iostream>
#include <string>
#include <sstream>
#include <ctime>

std::string dmformat(unsigned int);
std::string yformat(unsigned int);

class Calendar
{
public:

    std::string toString()
    {
        std::stringstream stream;
        std::format;
    };
    
private:
    std::time_t time;
};

int main(int argc, char const *argv[])
{
    
    return 0;
}
